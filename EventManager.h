#ifndef EVENTMANAGER_H
#define EVENTMANAGER_H

class EventManager
{

public:
	/*
	EventManager();
	virtual ~EventManager();
	void createEvent(EventCondition, Event);
	void listAffectedEvents(EventCondition);
	void triggerEvents(EventCondition);
	void clearEvent(EventCondition);
	void clearEvent(Event);
	*/
};

#endif